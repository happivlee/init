#6. Get the complete path of the file that contains the IP address of the DNS server you’re using

cat /etc/resolv.conf

#the command for just seeing the IP Address of the DNS Server is: scutil --dns
